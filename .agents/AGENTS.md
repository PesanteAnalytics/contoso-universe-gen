# Agent Policies — contoso-universe-gen

> These policies govern all AI agent behavior within this repository.
> Auto-generated by the Skill Factory. Customize as needed.

## 🔒 Storage Rules

### Skills
- ✅ **Required**: All skills MUST be stored in `<repo>/.agents/skills/<skill-name>/`
- ❌ **Prohibited**: NEVER save skills to global AI memory locations
- If asked to "remember" or "save" a skill, place it in `.agents/skills/`

### Plans & Tasks
- ✅ **Required**: All plans and tasks MUST be stored in `<repo>/.agents/plans/`
- ✅ **Naming**: `YYYY-MM-DD_descriptive-kebab-name.md`
- ✅ **Auto-organize**: Create `.agents/plans/` and files proactively — do NOT ask the user
- ❌ **Prohibited**: NEVER save plans/tasks to the AI brain directory or global memory

## 🛠️ Skill Creation

- **Mandatory**: Use the global `skill-factory` skill for ALL new skill creation
- **Reference**: Use `anthropics-skills-ref` as the immutable source of community patterns
- **Quality Gate**: No skill is "done" until it passes the 8-point Quality Checklist
- **Portability**: Zero hardcoded paths — use relative paths and runtime discovery

## 📁 Repository Hygiene

- Agent-specific files go in `.agents/` — separate from project source code
- Scripts used by skills but useful independently go in `scripts/` at project root
- Configuration files go in `configs/` at project root
- Never place agent artifacts in `src/`, `output/`, or other project directories

## 🔍 Reference Repository

- `anthropics-skills-ref` is **read-only** for agents
- Allowed: Read files, copy content out, execute scripts in sandboxed environments
- Prohibited: Creating, modifying, or deleting any file within it
- Updates: Done via `git pull` by a human only

